archivesBaseName = "comz-1.20.r4-support"

dependencies {
    implementation project(':API')
    implementation group: 'org.spigotmc', name: 'spigot', version: '1.20.6-R0.1-SNAPSHOT'
}

repositories {
    mavenCentral()
}

sourceCompatibility = targetCompatibility = JavaLanguageVersion.of(21)

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}